// The one entrance/interaction curve used across the site's motion — so
// every reveal, hover, and press reads as the same designed language
// instead of each component inventing its own timing.
export const EASE = [0.16, 1, 0.3, 1] as const
